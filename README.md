# MetroPark_Repo
MetroPark: A modern management system designed to optimize space utilization, enhance security, and streamline payment processes for urban parking facilities. This repository houses both the backend API and the frontend user interface.

FAQs:
🚀 Getting Started and Collaboration Guide
Follow these steps to set up your local development environment and start contributing to the MetroPark project.

1. ⬇️ Clone the Repository
Open your terminal or command prompt and run the following command to download the project files:

Using Bash or your chosen terminal

git clone https://github.com/PabloEcobag-alt/MetroPark_Repo.git
cd MetroPark_Repo
---------

2. ⚙️ Setup Environment (Backend & Frontend)
Navigate into the respective directories (backend or frontend) to install necessary dependencies.

For Backend:
Using Bash or your chosen terminal

cd backend
# INSERT SPECIFIC BACKEND SETUP COMMAND (e.g., pip install -r requirements.txt, npm install, etc.)

For Frontend:
Using Bash or your chosen terminal
cd frontend
# INSERT SPECIFIC FRONTEND SETUP COMMAND (e.g., npm install, yarn install, etc.)
--------

3. 🌳 Start Working on a Feature
Always create a new feature branch before making any changes. This keeps the main branch (main or master) stable.

Pull the latest changes to ensure your local main branch is up-to-date:

Using Bash or your chosen terminal

git checkout main
git pull


Create your new feature branch:
Using Bash or your chosen terminal

git checkout -b feature/your-feature-name 
# Example: git checkout -b feature/parking-status-api
---------

4. ✅ Commit and Push Your Changes
Once you've made progress or completed your feature, follow these steps:

Stage your changes:
Using Bash or your chosen terminal

git add .


Commit with a clear, descriptive message:
Using Bash or your chosen terminal

git commit -m "feat: Briefly describe the new feature or fix (JIRA-XYZ)"

Commit Tip: Use prefixes like feat: (new feature), fix: (bug fix), or refactor: (code cleanup) for clarity.

Push your branch to the remote repository:

Using Bash or your chosen terminal

git push origin feature/your-feature-name
